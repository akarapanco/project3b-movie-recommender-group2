#include <vector>
#include <unordered_map>
#include "models.h"

// ISA implement  getRecommendations() using KNN similarity algorithm

std::vector<int> getRecommendations(
    int targetUserId,
    const std::unordered_map<int, User>& users,
    int k
) {
    // ISA write full KNN algorithm here
    return {}; 
}
